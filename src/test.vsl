PROTO INT add(a, b)
PROTO INT sub(a, b)

FUNC INT add(a, b) {
	INT x
	INT t[3]
	t[0] = 1
	t[1] = 2
	t[2] = 3
	x:=a+b
	RETURN x
}

FUNC VOID main(a, b) {
	INT x
	INT y
	READ x
	IF x THEN x:=2 ELSE x:=0 FI
	WHILE x+add(x, y) DO x:= a - b DONE
	add(x, y)
	PRINT x
}
